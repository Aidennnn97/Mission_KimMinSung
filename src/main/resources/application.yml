spring:
  mvc:
    hidden-method:
      filter:
        enabled: true

  profiles:
    active: dev
    include:
      - secret2

logging:
  level:
    root: INFO
    com.ll.gramgram: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE